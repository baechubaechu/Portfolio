# 나중에 해볼 것들

작업 노트가 아니라 **아직 안 넣은 기능 구상**이다. 지금 히어로는 천구를 고개로 돌리며 프로젝트를 바라보는 별자리고, 호버하면 카드가 뜬다. 아래는 그 위에 얹을 수 있는 것들.

우선순위는 느슨하다. 위에 있을수록 지금 구조와 잘 맞고, 아래로 갈수록 욕심이다.

---

## 천구 / 시점

- **살짝 줌.** 지금 고개만 돌린다. 휠이나 트랙패드로 한 별자리에 다가가면 “망원경” 느낌이 난다. 너무 자유 카메라가 되면 슬라이드처럼 보일 수 있으니, 시야각만 좁히는 쪽이 안전하다.
- **자동 일주.** 한동안 가만히 있으면 하늘이 아주 천천히 돈다. 호버·카드가 뜨는 순간 멈춘다.
- **별자리 이름.** 바라보는 순간 도형 옆에 `Deary` 같은 학명이 희미하게 뜬다. 카드와 겹치지 않게 도형 반대쪽.
- **더 깊은 먼지.** 먼 별 레이어를 하나 더 두고 시차에 차이를 주면 창이 두꺼워 보인다.

## 탐색

- **속성으로 거르기.** `Code`를 바라보거나 누르면 그 속성을 가진 프로젝트만 남고 나머지는 더 희미해진다. 지금 속성 카드가 이미 이웃 목록을 보여 주니, 그걸 하늘 필터로 확장하면 된다.
- **해시 링크.** `#deary`로 들어오면 그 별을 바라본 상태로 시작한다. 포트폴리오를 메일로 보낼 때 쓸모 있다.
- **키보드.** Tab은 이미 노드를 돈다. `← →`로 프로젝트만 순회하고, 카드의 View project에 포커스가 바로 가게.
- **미니맵.** 지금은 가장자리 별이 잘린다. 구석에 아주 작은 천구 스케치만 두면 내가 어디를 보는지 안다.

## 카드 / 정보

- **작은 이미지.** 카드에 대표 컷 한 장. 호버 순간에만 뜨니까 무거우면 안 된다. 썸네일 한 장이면 충분하다.
- **한 줄 로그라인.** 지금 카드는 속성과 가중치가 중심이다. 제목 아래 한 문장이 있으면 건축/연구 맥락이 바로 읽힌다. 데이터는 `portfolio.js`에 `blurb` 하나면 된다.
- **카드가 별을 가리지 않는 경로.** 카드로 커서를 옮길 때 빈 하늘을 지나면 호버가 풀린다. 마지막 바라본 프로젝트를 스테이지 안에 있는 동안 유지할지 결정해야 한다.
- **상세로 넘어갈 때 카메라 유지.** 이미 `is-leaving`이 있다. 별을 중심으로 줌인하며 `project-detail`로 이어지면 맵과 페이지가 한 장면이 된다.

## 콘텐츠

- **X-tra Space 상세.** `href`가 비어 있다. 페이지가 생기면 별에서 바로 들어간다.
- **진행 중 표시.** 하늘에서 `In progress`가 카드에만 있다. 별 옆에 아주 작은 점선 고리가 있으면 아직 열린 작업인지 보인다.
- **연도를 거리로.** 최근 작업이 더 가깝고, 오래된 작업이 조금 더 안쪽에 있게. 레이아웃을 연도로 물들이는 정도면 된다. 축을 명시할 필요는 없다.
- **작업 수 늘어나면 성군.** 프로젝트가 열 개를 넘으면 학교 / 공모 / 개인을 하늘 구역으로 나눈다. 지금은 개수가 적어서 한 창이 맞다.

## 모바일 / 접근성

- **터치.** 호버가 없다. 탭하면 별자리+카드, 같은 별을 다시 탭하면 상세. 드래그는 고개 돌리기.
- **축소 동작.** `prefers-reduced-motion`은 이미 있다. 천구 투영을 끄고 평면 맵으로 떨어지는 모드를 더 분명하게.
- **스크린 리더.** 별자리 SVG와 아래 섹션 카드가 같은 프로젝트를 두 번 읽는다. 히어로를 `aria`로 요약하고 본문 목록과 연결.

## 표현 (욕심)

- **실제 별 카탈로그 느낌.** 등급(밝기)을 작업 규모나 역할 비중에 매핑. 이미 weight가 있다.
- **짧은 사운드.** 바라볼 때 아주 작은 틱, 카드가 열릴 때 한 음. 기본은 음소거.
- **WebGL 하늘.** SVG로 별·선은 충분하다. 유성, 대기 산란, 더 많은 먼지를 원할 때만.

---

데이터만 손대려면 `js/constellation/data/portfolio.js`다. 시점·투영은 `js/constellation/lib/sky.js`, 별자리 도형은 `lib/asterism.js`, 호버 카드는 `components/ProjectInfo.js`.
